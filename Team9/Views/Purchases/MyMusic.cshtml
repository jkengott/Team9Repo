@model IEnumerable<Team9.Models.Song>
@using Team9.Models


@{
    ViewBag.Title = "MyMusic";
}

<h2>My Music</h2>

@using (Html.BeginForm())
{
    <p class="form-group">
        Search: @Html.TextBox("SongString", null, new { @class = "form-control" }) <br />
        <input type="submit" value="Search" class="btn btn-default" />
        @Html.ActionLink("Search By Genre", "MyMusicSearchResults", null, new { @class = "btn btn-default" })
        @Html.ActionLink("Show All", "MyMusic", null, new { @class = "btn btn-default" })

    </p>
}


<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.SongName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.SongArtist)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.SongGenre)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.SongAlbum)
        </th>


        @foreach (var item in Model)
        {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.SongName)
            </td>

            <td>
                @foreach (Artist a in item.SongArtist)
                {
                    <span>@a.ArtistName *</span>
                }
            </td>

            <td>
                @foreach (Genre a in item.SongGenre)
                {
                    <span>@a.GenreName *</span>
                }
            </td>

            <td>
                @Html.DisplayFor(modelItem => item.SongAlbum.AlbumName)
            </td>



        </tr>

    }

</table>